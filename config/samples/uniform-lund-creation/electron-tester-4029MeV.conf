# electron-tester-4029MeV.conf -----------------------------------------------------------------
# Description:
#   Legacy-derived trigger-electron angular tester for Ar40 metadata at 4.02962 GeV.
#
# Purpose:
#   Scan 5--40 degrees and full phi at beam momentum to estimate the trigger-electron placement used by electron-hadron samples.
#
# Workflow:
#   source run.csh --workflow create-lund --source uniform \
#     --config config/samples/uniform-lund-creation/electron-tester-4029MeV.conf --output OUTPUT_PARENT
#
# Units:
#   Beam energy and particle momenta are GeV and GeV/c; angles are degrees; vertices are cm.
#
# Notes:
#   This profile is complete except for output. A command-line --key value overrides the matching value below.

# Sample identity -------------------------------------------------------------
# The tester writes one electron at the selected beam energy; it is an angular scan rather than an interaction model.

channel = electron-tester
beam-energy = 4.02962

# Target geometry and metadata ------------------------------------------------
# `rgm-target` resolves target geometry, A/Z metadata, and the default GEMC variation.
# The maintained tester samples the selected target geometry like every other maintained sample.

rgm-target = Ar40

# Run size and reproducibility ------------------------------------------------
# `events` is the total tester count and `events-per-file` controls splitting.
# `seed` drives electron angles; `vertex-seed` independently drives target-geometry sampling.

events = 1000000
events-per-file = 25000
seed = 67890
vertex-seed = 12345

# Output contract -------------------------------------------------------------
# Tester filenames are derived automatically. The writer uses the project LUND layout and rounded masses, then always writes ROOT/PDF/PNG monitoring artifacts.

# Tester electron kinematics --------------------------------------------------
# Beam mode fixes electron momentum at the beam value while theta scans 5--40 degrees and phi covers the full azimuth.
# The stored momentum limits document the supported range but beam mode uses the configured beam energy directly.

electron-theta-min = 5
electron-theta-max = 40
electron-p-min = 0.7
electron-p-max = 4.02962
