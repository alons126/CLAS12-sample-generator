# Archived imported sources

These directories preserve the original repositories for source comparison. They are excluded from the supported build and are not runnable migration entry points. Historical wrappers contain hardcoded paths and destructive cleanup commands.

Use the root build and commands in [the newcomer guide](../docs/index.md). Detector gcard/YAML resources have moved to `config/detector/`; the remaining old submission scripts are historical references only.
